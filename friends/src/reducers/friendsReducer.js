
const initialState = {
  friends: [],
}

export const friendsReducer = (state = initialState, action) => {
  switch(action.type) {
    //return cases here with actions
    default:
      return state;
  }
  
}